#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    # تأمين وجود عنصرين على الأقل عبر الدمج مع أصفار ثم أخذ أول عنصرين
    a = tuple_a + (0, 0)
    b = tuple_b + (0, 0)
    
    # جمع العناصر وإرجاع مجموعة جديدة من عنصرين
    return (a[0] + b[0], a[1] + b[1])
